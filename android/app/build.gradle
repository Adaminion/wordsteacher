plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id "com.google.gms.google-services" // Firebase
}

android {
    namespace = "com.adaminion.memorly"
    compileSdk = 35 // This is modern, good
    ndkVersion = flutter.ndkVersion

    defaultConfig {
        applicationId = "com.adaminion.memorly"
        minSdk = 23
        targetSdk = 34 // Consider updating to 35 to match compileSdk if appropriate
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    // Solution: Align Java compatibility with Kotlin's jvmTarget
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17 // CHANGE THIS
        targetCompatibility = JavaVersion.VERSION_17 // CHANGE THIS
    }

    kotlinOptions {
        jvmTarget = "17" // This is fine
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // For actual releases, you'll need a proper release signing config
        }
    }
}

flutter {
    source = "../.."
}